import { InputWrapper, OutputWrapper } from "./style";

const RadioButton = ({customStyle}) =>{

    return(
        <OutputWrapper customStyle={customStyle}>
            <InputWrapper/>
        </OutputWrapper>
    )
}

export default RadioButton;